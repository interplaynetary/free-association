// Test importing from the workspace package
import { computeAllocations, itcSeed } from '@playnet/free-association';

console.log('✅ computeAllocations:', typeof computeAllocations);
console.log('✅ itcSeed:', typeof itcSeed);
console.log('');
console.log('The package is available via workspace!');
