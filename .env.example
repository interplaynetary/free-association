# Free Association Docker Environment Variables
# COPY THIS FILE TO .env AND FILL IN SECURE VALUES

# ========================================
# CRITICAL: SECURITY CREDENTIALS
# ========================================
# These MUST be set before running docker-compose!
# Generate secure values:
#   API Key: openssl rand -hex 32
#   JWT Secret: openssl rand -base64 48
#
# NOTE: Single MASTER_API_KEY is suitable for development.
# For production with multiple users, implement database-backed
# API key storage with rotation and per-user keys.

MASTER_API_KEY=
JWT_SECRET=

# ========================================
# Optional Configuration
# ========================================

# Environment (development or production)
NODE_ENV=development

# CORS - Comma-separated allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# JWT token expiry (examples: 24h, 7d, 30d)
JWT_EXPIRY=24h

# Body parser limit (default: 1mb)
# NOTE: May need to increase to 5mb for large recognition trees
# BODY_PARSER_LIMIT=5mb

# Request timeout in milliseconds (default: 5000)
REQUEST_TIMEOUT=5000

# ========================================
# Instructions
# ========================================
# 1. Copy this file: cp .env.example .env
# 2. Generate secure credentials:
#      MASTER_API_KEY: openssl rand -hex 32
#      JWT_SECRET: openssl rand -base64 48
# 3. Paste values above
# 4. Run: docker-compose up -d

OPENROUTER_KEYS=sk-or-v1-your-key-1,sk-or-v1-your-key-2
