# ====================================
# Free Association - Environment Configuration
# ====================================
# Copy this file to .env and fill in your values
# Generate secrets with: openssl rand -base64 32

# ====================================
# Authentication (REQUIRED)
# ====================================
JWT_SECRET=your-super-secret-jwt-key-change-me-use-openssl-rand-base64-32
JWT_EXPIRY=24h
MASTER_API_KEY=your-master-api-key-change-me-use-openssl-rand-hex-32

# ====================================
# OpenRouter Configuration (REQUIRED for AI features)
# ====================================
# Get keys from: https://openrouter.ai/keys
# Format: comma-separated list of keys
OPENROUTER_KEYS=sk-or-v1-your-key-1,sk-or-v1-your-key-2
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# ====================================
# Application Configuration
# ====================================
APP_URL=http://localhost:5173
NODE_ENV=development

# ====================================
# Gun Relay Configuration
# ====================================
# Gun database relay for decentralized data storage
GUN_RELAY_HOST=0.0.0.0
GUN_RELAY_PORT=8765
GUN_RELAY_STORE=true
GUN_RELAY_PATH=gun-data
GUN_RELAY_SHOW_QR=false

# ====================================
# Holster Relay Configuration
# ====================================
# Holster database relay for decentralized data storage
HOLSTER_RELAY_HOST=0.0.0.0
HOLSTER_RELAY_PORT=8766
HOLSTER_RELAY_STORAGE=true
HOLSTER_RELAY_STORAGE_PATH=./holster-data
HOLSTER_MAX_CONNECTIONS=100

# ====================================
# Optional: CORS Configuration
# ====================================
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173,https://your-domain.com

# ====================================
# Quick Setup Instructions:
# ====================================
# 1. Copy this file: cp env.example.txt .env
# 2. Generate secrets:
#    JWT_SECRET=$(openssl rand -base64 32)
#    MASTER_API_KEY=$(openssl rand -hex 32)
# 3. Add your OpenRouter keys
# 4. Start the server: bun run dev

